# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/my_mail
    username: root
    password: rootroot

# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.quickstart: debug